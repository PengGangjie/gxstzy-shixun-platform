# 版本锁定：与本地验证过的组合一致（pip install --no-cache-dir 部署可复现）
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-dotenv==1.2.2
logto==0.2.1
libsql-client==0.3.1
itsdangerous==2.2.0
python-multipart==0.0.32
openpyxl==3.1.5
pillow==12.2.0
# CAS 验票出网调用（本地 venv 隐式存在，容器无此依赖曾致启动崩溃）
httpx==0.28.1
